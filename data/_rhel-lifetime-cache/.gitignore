# Per-year RHSA pull from Red Hat's Security Data API. Re-generated by
# scripts/fetch_rhel_lifetime_advisories.py on demand. Large (~50MB total
# across 17 years × thousands of advisories per year).
# Only canonical aggregates are committed: data/rhel-lifetime-advisories.json
# and data/rhel-lifetime-kernel-advisories.json
csaf-*.json
